实现一个类似Linux系统下的pstree命令，可以接受-n，-V，-p三个参数
3.26	周二
	下午花了一个多小时才把jyy的操作系统实验给整好，遇到两个问题，一个是没有TOKEN没法make，到上一层文件夹里把makefile里的if注释掉就好了，一开始没发现是../下的，还纳闷第4行啥都没有为啥报错。二是64位机上没有32位的库，GPT也不知道，上bing一搜两分钟就解决了，甚至还没用上Google，所以GPT还不是万能的，以后还得优先用搜索引擎。
	下午把报错弄完后写了M1的输入，晚上大概想好怎么获取pid和父进程了，剩下的周末再看。

3.29	周五
	花了一整个下午做完了M1，往C:\Program Files (x86)\Windows Kits\10\Include\10.0.22621.0\ucrt\添加了一个Linux中的头文件dirent.h。
	整体实现：从每个status中读出name、pid和ppid，新建节点，构建一棵树，每个节点使用一个指针数组存储它的孩子，一个变量存储孩子的数量，输出时直接从根节点遍历整棵树，根据参数微调输出即可。

4.6	周六
	今天发现M1的空格有问题，晚上改了一会，差不多改好了，但是还有问题，下次改的话估计往每个节点存一个空格长度变量，存一个父节点指针，然后打印相应长度的空格。